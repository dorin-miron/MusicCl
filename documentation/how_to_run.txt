OS used Ubuntu 16.04 LTS (64)

python version 2.7.12

# make all updates
sudo apt-get update

#install git
sudo apt-get-install git

# clone repository

git clone https://github.com/dorin-miron/MusicCl.git

# download the dataset
wget http://opihi.cs.uvic.ca/sound/genres.tar.gz

# extract dataset

tar - xvzf genres.tar.gz

# insert path to dataset in onfing.cfg
	open MusicCl/config.cfg
	insert corect GENRE_DIR

# install pip
sudo apt-get install python-pip
pip install --upgrade pip

# install pydub
sudo pip install pydub
sudo apt-get install python-numpy python-scipy python-matplotlib

sudo apt-get install ffmpeg

################
# make conversion from au to wav

python converter.py (91 seconds)

sudo apt-get update
# sudo apt-get install build-essentioal python-dev python-setuptools libatlas-dev
sudo pip install -U scikit-lean
sudo pip install scikits.statmodels
sudo pip install scikits.talkbox

#########
# extract features from dataset

python ceps.py (308 s)
python classifier.py (130 s)




versions
_________________

adium-theme-ubuntu==0.3.4
cycler==0.9.0
decorator==4.0.6
matplotlib==1.5.1
numpy==1.11.0
Pillow==3.1.2
pydub==0.18.0
pyparsing==2.0.3
python-dateutil==2.4.2
pytz==2014.10
scikit-learn==0.18.1
scikits.statsmodels==0.3.1
scikits.talkbox==0.2.5
scipy==0.17.0
six==1.10.0
unity-lens-photos==1.0

sa modific caractreristici/ atribute -: trasaturi.

deep learning -> invatare de profunzime CNN
state of the art / related working



